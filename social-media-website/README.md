# Social Media Website

This folder contains a starter social media app with a Vite + React client and an Express + MongoDB server.

## Client

```bash
cd client
npm install
npm run dev
```

## Server

```bash
cd server
npm install
npm run dev
```

Update `server/.env` with your MongoDB connection string and JWT secret before running the API.
